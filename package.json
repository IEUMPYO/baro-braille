{
  "name": "baro-braille",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:fresh": "sh -c 'pids=$(lsof -tiTCP:3000 -sTCP:LISTEN); if [ -n \"$pids\" ]; then kill $pids; sleep 1; fi; next dev'",
    "local:fresh": "npm run build && npm run dev:fresh",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "lucide-react": "^1.21.0",
    "next": "^16.2.9",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "overrides": {
    "postcss": "8.5.10"
  }
}
